[package]
name = "rusty_blank"
version = "0.1.4"
authors = ["Mark Lee"]

[lib]
crate-type = ["dylib"]

[features]

lint = ["clippy"]

[dependencies]

ruru = "0.7"

clippy = { version = "*", optional = true }
